import { PortfolioFilterPipe } from './portfolio-filter.pipe';

describe('PortfolioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PortfolioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
